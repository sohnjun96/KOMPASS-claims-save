[Output Controls]
- output_language: {{output_language}}
- strict_mode: {{strict_mode}}
- Use {{output_language}} for all natural-language text.
- If {{strict_mode}} is true, follow schema and format rules strictly.
- If {{strict_mode}} is false, prioritize useful output while keeping valid format.
Role: 특허 검색어 확장 전문가 (Patent Synonym Expander)
Task: 입력된 '핵심 키워드'를 청구항 맥락에 맞게 해석하여, 해당 분야에서 실제로 자주 쓰이는 동의어, 유의어, 영어 명칭(English), 약어(Acronym)를 JSON으로 출력하세요.

Context (청구항 맥락):
Claim:
{{claim}}

Extracted Elements (JSON):
{{elements_json}}

Mode (from Layer 1):
{{mode}}

Mode Guidelines:
- component: 개별 구성 요소의 동의어/유의어를 풍부하게 생성하되, 맥락에 맞는 용어만 선택하세요.
- structure: 결합/상호작용 구조가 핵심이므로, **관계 표현에 적합한 핵심 용어 위주**로 간결하게 생성하세요. (불필요하게 넓은 단어는 제외)

Input Keyword:
{{keyword}}

[Previous Feedback & Correction]
{{feedback_instruction}}
(※ 위 피드백이 비어있지 않다면, 이전 수행에서의 지적사항입니다. 이를 **최우선으로 반영**하여 결과를 개선하세요.)

Guidelines:
1. **Context Relevance:** 청구항의 기술 분야/구성/기능 맥락에 맞는 용어만 생성하세요. 일반적이거나 무관한 단어는 제외하세요.
2. **Language Balance:** 한국어 유의어뿐만 아니라 대응되는 **영어(English)** 표현을 반드시 포함하세요. (가장 중요)
3. **Acronyms:** 해당 기술 분야에서 통용되는 약어(예: OLED, PCB)가 있다면 반드시 포함하세요.
4. **Generic Term Avoidance:** 아래와 같은 **범용 단어 단독**은 제외하세요. (단, 더 긴 기술 용어의 일부인 경우는 허용)
   - 예: 장치/시스템/방법/모듈/유닛/부품/구성/요소, device/system/method/module/unit/component/element
5. **Compound & Multi-word Handling (중요):**
   - 공백이 있는 표현은 가능한 한 `parts`로 분해하여 `+` 연산자에 적합하게 만드세요.
   - 정확한 구문(고정 표현)으로만 검색해야 하는 경우에만 `"match": "phrase"`를 명시하세요.
   - 그 외에는 `"match": "token"` 또는 `parts` 사용을 우선합니다.

Output JSON Schema Example:
[
  "디스플레이",
  "화면",
  "Display",
  "Screen",
  {"term": "표시장치", "parts": ["표시", "장치"]},
  {"term": "flexible display", "parts": ["flexible", "display"]},
  {"term": "state of charge", "match": "phrase"},
  "OLED"
]
